$$ CANONICAL WINDBG WORKSHOP COMMAND CATALOG
$$
$$ The Setup Agent and Prompt must copy commands from this file verbatim. They must
$$ not compose, abbreviate, or infer debugger commands. Run one command per WinDbg
$$ MCP execution and preserve this order unless a command is explicitly conditional.
$$
$$ The only dynamic command is thread selection. Use the exact DML selection command
$$ returned by the current !us logwriter output; never construct or reuse a thread ID.

$$ 01 - Inspect the current symbol path.
.sympath

$$ 02 - Set the approved symbol path only when the current session needs correction.
.sympath srv*C:\symbol*https://symweb.azurefd.net;cache*C:\symbol;srv*https://symweb.azurefd.net

$$ 03 - Force symbol reload only after the symbol path was corrected.
.reload /f

$$ 04 - Open a private command log only when requested and the target will not overwrite evidence.
.logopen /t C:\temp\windbg-workshop.txt

$$ 05 - Inspect command-log state.
.logfile

$$ 06 - Close the command log.
.logclose

$$ 07 - Load MEX.
.load C:\tools\SqlDebugWorkshop\extensions\mex.dll

$$ 08 - Load WinDbgCs from its installed package directory.
.load C:\Program Files\PackageManagement\NuGet\Packages\WinDbgCs.3.2.7\WinDbgCsExt.dll

$$ 09 - Verify extension registration and exact paths.
.chain

$$ 10 - Discover commands from the loaded MEX version.
!mex.help

$$ 11 - Find the Log Writer candidate thread.
!us logwriter

$$ 12 - Select the matching thread using only the exact current DML command returned by command 11.
$$ DYNAMIC_RUNTIME_COMMAND_FROM_PREVIOUS_OUTPUT

$$ 13 - Show the native stack for the selected thread.
k

$$ 14 - Show the MEX raw stack scan for the same thread.
!mex.t -raw

$$ 15 - Verify the SQL Server build before selecting dscript.
lmv m sqlservr

$$ 16 - Discover WinDbgCs scripts and runtime-advertised actions.
!execute

$$ 17 - Conditional: run only when command 16 advertises this exact action and scripts are absent.
!execute ExternalScripts.Install ;

$$ 18 - Final extension verification.
.chain
